#Some default classes
# Look into config to see how powerful this is and where to set what

trigger=LList.pause
generator=memeograph.generator.jdi.JDI

renderer=memeograph.renderer.processing.MemeoFrame

filters=java.*,javax.*,sun.*

verbose=true
useopengl=true

#Should we link the primitives together? By default, we don't.
#groupprimitives=true
